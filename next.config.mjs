/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["spotlight.tailwindui.com", "media.licdn.com"], // Add the domains here
  },
  // Any other configurations you have
};

export default nextConfig;
